name: Release

# 打 tag 自动发版：git tag vX.Y.Z && git push origin vX.Y.Z
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Publish single-file
        shell: pwsh
        run: ./publish.ps1

      - name: Extract version from tag
        shell: pwsh
        id: version
        run: |
          # 从 tag 提取版本（v0.1.2 → 0.1.2），并生成带版本的文件名
          $tag = "${env:GITHUB_REF}" -replace '^refs/tags/', ''
          $ver = $tag.TrimStart('v')
          "version=$ver" >> $env:GITHUB_OUTPUT
          "setup_name=WinNetFix-Setup-$ver.exe" >> $env:GITHUB_OUTPUT
          "exe_name=WinNetFix-$tag.exe" >> $env:GITHUB_OUTPUT
          "zip_name=WinNetFix-$tag.zip" >> $env:GITHUB_OUTPUT
          Write-Host "tag=$tag ver=$ver"

      - name: Rename exe with version (portable)
        shell: pwsh
        run: |
          $src = './publish/WinNetFix.exe'
          $dst = "./publish/${{ steps.version.outputs.exe_name }}"
          Copy-Item $src $dst -Force
          Get-Item $dst | Select-Object Name, Length

      - name: Create portable zip
        shell: pwsh
        run: |
          $zip = "./publish/${{ steps.version.outputs.zip_name }}"
          $staging = Join-Path $env:RUNNER_TEMP "winnetfix-portable"
          New-Item -ItemType Directory -Path $staging -Force | Out-Null
          Copy-Item './publish/WinNetFix.exe' (Join-Path $staging 'WinNetFix.exe')
          Compress-Archive -Path (Join-Path $staging 'WinNetFix.exe') -DestinationPath $zip -Force
          Get-Item $zip | Select-Object Name, Length

      - name: Install Inno Setup
        shell: pwsh
        run: |
          # 下载并静默安装 Inno Setup 6
          $installer = Join-Path $env:TEMP 'innosetup.exe'
          Invoke-WebRequest -Uri 'https://jrsoftware.org/download.php/is.exe' -OutFile $installer
          Start-Process $installer -ArgumentList '/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART' -Wait
          Get-Item 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe' | Select-Object FullName

      - name: Build Setup installer
        shell: pwsh
        run: |
          $iscc = 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe'
          & $iscc "./installer/WinNetFix.iss" "/DMyAppVersion=${{ steps.version.outputs.version }}"
          Get-ChildItem ./publish/WinNetFix-Setup-*.exe | Select-Object Name, Length

      - name: Generate SHA256
        shell: pwsh
        run: |
          $assets = @(
            "./publish/${{ steps.version.outputs.setup_name }}",
            "./publish/${{ steps.version.outputs.exe_name }}",
            "./publish/${{ steps.version.outputs.zip_name }}"
          )
          $lines = foreach ($f in $assets) {
            $hash = Get-FileHash -Algorithm SHA256 $f
            "$(Split-Path $f -Leaf)  $($hash.Hash)"
          }
          $lines | Set-Content ./publish/SHA256.txt
          Get-Content ./publish/SHA256.txt

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            publish/WinNetFix-Setup-*.exe
            publish/WinNetFix-v*.exe
            publish/WinNetFix-v*.zip
            publish/SHA256.txt
          draft: true
          generate_release_notes: true
